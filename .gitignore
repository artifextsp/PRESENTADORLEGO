# Los modelos 3D son muy pesados para GitHub (>50MB)
# Cárgalos desde la interfaz de la plataforma directamente
assets/*.obj
assets/*.mtl
# assets/*.glb  ← permitido para que el visor de jueces cargue el modelo
assets/*.dae
.DS_Store
